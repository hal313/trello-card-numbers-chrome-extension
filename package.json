{
  "name": "trello-card-numbers-chrome-extension",
  "description": "Chrome extension for displaying card numbers on Trello cards",
  "version": "0.0.10",
  "dependencies": {
    "jquery": "^3.3.1",
    "lodash": "^4.17.5"
  },
  "scripts": {
    "start": "grunt debug",
    "build": "grunt build",
    "release": "grunt release",
    "publish": "grunt publish --clientId=$CLIENT_ID --clientSecret=$CLIENT_SECRET --refreshToken=$REFRESH_TOKEN"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hal313/trello-card-numbers-chrome-extension.git"
  },
  "devDependencies": {
    "chrome-extension-deploy": "^3.0.0",
    "grunt": "^0.4.5",
    "grunt-bump": "^0.8.0",
    "grunt-chrome-manifest": "^0.2.4",
    "grunt-contrib-clean": "^0.5.0",
    "grunt-contrib-compress": "^0.9.1",
    "grunt-contrib-copy": "^0.5.0",
    "grunt-contrib-jshint": "^0.9.2",
    "grunt-contrib-uglify": "^0.4.1",
    "grunt-contrib-watch": "^0.6.1",
    "jshint-stylish": "^0.1.5",
    "load-grunt-tasks": "^3.5.2",
    "time-grunt": "^0.3.2"
  },
  "license": "MIT",
  "engines": {
    "node": ">=0.8.0"
  }
}
